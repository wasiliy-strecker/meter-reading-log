# Improve meter actions, photo history, and reminders

- Removed the extra top and bottom expansion borders around `Frühere Fotos`
  while preserving the rounded card and its internal content divider.
- Replaced the meter-detail kebab menu with visible full-width `Bearbeiten` and
  `Zähler löschen` actions directly below the summary card.
- Added daily and weekly reading reminders, including weekday selection,
  recurring Android schedules, persisted values, and clear detail summaries.
- Added focused widget and scheduling tests for all three changes.

