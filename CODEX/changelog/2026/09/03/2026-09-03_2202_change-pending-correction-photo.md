# Allow changing a pending correction photo

- Moved the replacement action directly below the newly selected correction
  photo so it is visible before the informational and OCR sections.
- Renamed the action to `Korrekturfoto ändern` and kept both camera and gallery
  available when replacing an accidentally selected image.
- Added widget coverage for selecting a second correction photo before saving.

