# Interner Test-Release 1.0.0 (Build 7)

- Buildnummer von `1.0.0+6` auf `1.0.0+7` erhöht; Marketing-Version unverändert.
- Enthält alle seit Build 6 abgeschlossenen Änderungen: vollständigere Foto-/OCR-Korrekturen und historische Zählerangaben in PDFs, Ablesezeiten anhand des gespeicherten UTC-Offsets, mehrseitige lange Notizen und Korrekturtexte, neuer Hinweis beim Zähleranlegen sowie gekürzte Datenschutz- und Korrekturhinweise.
- Prüfung: `dart format lib test` (106 Dateien, unverändert), `flutter analyze --no-pub` und beide vollständigen Testsuiten erfolgreich: jeweils 192 Tests mit `flutter test --no-pub --dart-define=PDF_TEXT_AUDIT=true`, zusätzlich mit `--flavor store`. PDF-Inhalte einschließlich langer Texte mit `pdftotext` geprüft; `git diff --check` erfolgreich.
- Store-AAB mit `FLUTTER_BIN=/home/unknown/.local/flutter/bin/flutter ./scripts/build_internal_test_aab.sh` gebaut; keine Abhängigkeiten aktualisiert. Benannte Lieferung bytegleich mit `build/app/outputs/bundle/storeRelease/app-store-release.aab`.
- `bundletool validate` erfolgreich. Manifest direkt aus der benannten AAB geprüft: Paket `com.appfactory.meter_reading_log`, Version `1.0.0`, Code `7`, Label `ZählerstandLog`, kein Debug-Flag, keine `INTERNET`-/`ACCESS_NETWORK_STATE`-Berechtigung, `allowBackup=false`. Gradle-Metadaten bestätigen `storeRelease`.
- `jarsigner -verify`: `jar verified`. Upload-Zertifikat unverändert gegenüber Build 6 (SHA-256 `1D:64:D5:0C:CF:5F:20:E8:BC:81:85:37:E4:10:4D:22:65:74:6F:78:35:C4:6A:9F:81:FF:85:05:1A:25:7B:55`). Bestehende Hinweise zu selbstsigniertem Zertifikat/Vertrauenskette, fehlendem Zeitstempel, POSIX-Attributen und JarInputStream-Reihenfolge weiterhin vorhanden.
- Lieferung: `build/releases/internal-test/zaehlerstandlog-1.0.0-build-7-internal-test.aab`, 69.433.004 Bytes; SHA-256 `bcd7afc4223bd40ae296a9aa03f9797f50795a13484f464665c7a31443467f58`. Frühere benannte Artefakte erhalten, Ausgabeordner geöffnet.
- Bekannter separater Fehler weiterhin offen: `RangeError` in `printing` / `PdfPreviewRaster._raster` bei der Dev-PDF-Vorschau; dieser Release-Auftrag umfasst keinen Vorschau-Fix und keinen Geräte-Smoke-Test.
- Keine Dev-APK gebaut, keine Installation, kein Reload/Neustart oder Datenumzug. Dev-/Play-Installationen und laufende Dev-Sitzung unverändert. Kein Play-Upload, keine Veröffentlichung und kein Push; AAB für manuellen internen Play-Upload bereitgestellt.
