const integrityProtectionTitle = 'Schutz vor nachträglichen Änderungen';

const correctionHistoryTitle = 'Korrekturverlauf';

const correctionHistoryText =
    'Hier findest du jede gespeicherte Korrektur mit Zeitpunkt, Grund und den geänderten Angaben.';

const integrityLimitationText =
    'Sie bestätigen jedoch nicht, wann oder von wem das Foto aufgenommen wurde.';

const technicalChecksTitle = 'Technische Prüfwerte anzeigen';

const pdfPurposeTitle = 'Wozu dient der PDF-Nachweis?';

const pdfPurposeText =
    'Die PDF bündelt Ablesung, Foto und Änderungen in einer Datei. Die App merkt sich ihren Prüfwert. Unter Einstellungen → PDF auf Änderungen prüfen kannst du später erkennen, ob genau diese PDF verändert wurde.';

const privateDocumentationText =
    'Sie ist eine private Dokumentation, keine amtliche Bestätigung und keine digitale Unterschrift.';

const historyPdfPurposeText =
    'Bündelt alle Ablesungen, Fotos und Korrekturen in einer PDF. Die App speichert einen Prüfwert, damit du später erkennen kannst, ob die Datei verändert wurde.';

const pdfVerificationTitle = 'PDF auf Änderungen prüfen';

const pdfVerificationText =
    'Wähle eine PDF aus. Die App vergleicht ihren Prüfwert mit dem Wert, der beim Erstellen auf diesem Gerät gespeichert wurde.';

const pdfMatchesTitle = 'Datei stimmt überein';
const pdfMatchesText =
    'Diese PDF stimmt mit der von dieser App gespeicherten Version überein.';

const pdfChangedTitle = 'Datei wurde verändert';
const pdfChangedText =
    'Zu diesem Dateinamen gibt es einen gespeicherten Nachweis, aber der Inhalt ist nicht mehr identisch.';

const pdfUnknownTitle = 'Kein Vergleichswert gespeichert';
const pdfUnknownText =
    'Die App hat auf diesem Gerät keinen gespeicherten Vergleichswert für diese PDF.';

const pdfIntegrityExplanation =
    'Der Prüfwert in diesem Dokument gehört zu den enthaltenen Daten. Wenn Foto oder Ablesung später verändert werden, passt der Prüfwert nicht mehr. Zusätzlich speichert die App beim Erstellen einen Prüfwert der fertigen PDF, mit dem Änderungen an der Datei erkannt werden können.';

const pdfPrivateDocumentationText =
    'Private Dokumentation · Keine amtliche Bestätigung und keine digitale Unterschrift.';
